---
phase: 06-ux-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - app/games/page.tsx
autonomous: true
requirements:
  - UX-02
  - UX-04
  - UX-05

must_haves:
  truths:
    - "Navigating to /games renders a games list page (not a 404)"
    - "When games.length === 0 after a successful fetch, /games shows an empty state card with a call-to-action link to /games/add"
    - "When fetchGames throws or returns non-ok, /games shows an error card with a retry button inline below the Header — not full-screen centered"
    - "The retry button calls fetchGames() directly — not window.location.reload()"
    - "While data is loading (isLoading === true), /games renders a skeleton layout matching the skeleton-shimmer pattern — not a plain 'Loading...' text"
    - "Header component is used with title 'My Games'"
  artifacts:
    - path: "app/games/page.tsx"
      provides: "Full games list page with complete lifecycle: loading, error, empty, content"
      exports: ["default GamesPage"]
      min_lines: 80
  key_links:
    - from: "app/games/page.tsx"
      to: "/api/games"
      via: "fetchGames useCallback calling fetch('/api/games')"
      pattern: "fetch.*api/games"
    - from: "app/games/page.tsx"
      to: "app/components/Header.tsx"
      via: "import Header from '@/app/components/Header'"
      pattern: "<Header.*title"
    - from: "error card"
      to: "fetchGames"
      via: "retry button onClick"
      pattern: "onClick.*fetchGames"
---

<objective>
Create app/games/page.tsx — a brand new file. This page does not exist yet (/games currently 404s).

Purpose: /games needs to be a fully functional page to satisfy UX-02 (skeleton loading), UX-04 (empty state), and UX-05 (error state with retry). This page is self-contained — it fetches all games from /api/games and renders its full lifecycle.

Output: app/games/page.tsx (new file — approximately 120 lines)
</objective>

<execution_context>
@C:/Users/jackl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jackl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-ux-polish/06-CONTEXT.md
@.planning/phases/06-ux-polish/06-RESEARCH.md
@.planning/phases/06-ux-polish/06-01-SUMMARY.md
@app/dashboard/page.tsx

<interfaces>
<!-- Header component (created in plan 01) -->
// app/components/Header.tsx
interface HeaderProps { title: string }
export default function Header({ title }: HeaderProps): JSX.Element
// Usage: <Header title="My Games" />

<!-- API response shape (from existing dashboard usage) -->
// GET /api/games
// Response: { games: Game[] }
interface Game {
  id: string;
  date: string;
  opponent: string;
  homeAway: string;
  result: string;
  goals: number;
  assists: number;
  shots: number;
  plusMinus: number;
  iceTime: number;
}

<!-- fetchGames pattern from dashboard/page.tsx (replicate exactly) -->
const fetchGames = useCallback(async () => {
  try {
    setIsLoading(true);   // ADD this — must reset for retry to work
    setError('');         // ADD this — clear previous error on retry
    const response = await fetch('/api/games');
    if (!response.ok) {
      if (response.status === 401) { router.push('/auth/login'); return; }
      throw new Error('Failed to fetch games');
    }
    const data = await response.json();
    setGames(data.games || []);
  } catch (err) {
    setError('Failed to load your games. Please try again.');
    console.error(err);
  } finally {
    setIsLoading(false);
  }
}, [router]);

<!-- Existing empty state from dashboard (lines 155-165) — use as pattern -->
<div className="bg-white rounded-lg shadow-sm border border-gray-200 p-12 text-center">
  <h2 className="text-2xl font-bold text-gray-900 mb-2">No Games Yet</h2>
  <p className="text-gray-600 mb-6">Start tracking your performance by adding your first game!</p>
  <Link href="/games/add" className="inline-block bg-blue-600 text-white px-6 py-3 rounded-lg font-medium hover:bg-blue-700 transition-colors">
    Add Your First Game
  </Link>
</div>

<!-- Existing error state from dashboard (lines 126-140) — upgrade: inline, not full-screen -->
<!-- Current bad pattern: if (error) return <full-screen-centered-div> -->
<!-- Required pattern: render Header first, then show error card inside main -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app/games/page.tsx — full games list page</name>
  <files>app/games/page.tsx</files>
  <action>
Create a brand new file. This file does NOT exist yet — do not try to read it first.

The page must implement all four lifecycle states: loading skeleton, error card, empty state, and content table. Structure it so the Header renders in ALL states (loading/error/empty/content) — the error card must appear INSIDE the page layout below the Header, not as a full-screen replacement. This is critical per Pitfall 4 in research.

**Required structure:**

```tsx
'use client';

import { useState, useEffect, useCallback } from 'react';
import Link from 'next/link';
import { useRouter } from 'next/navigation';
import { AlertCircle, ClipboardList } from 'lucide-react';
import Header from '@/app/components/Header';

interface Game {
  id: string;
  date: string;
  opponent: string;
  homeAway: string;
  result: string;
  goals: number;
  assists: number;
  shots: number;
  plusMinus: number;
  iceTime: number;
}

export default function GamesPage() {
  const router = useRouter();
  const [games, setGames] = useState<Game[]>([]);
  const [isLoading, setIsLoading] = useState(true);
  const [error, setError] = useState('');

  const fetchGames = useCallback(async () => {
    try {
      setIsLoading(true);
      setError('');
      const response = await fetch('/api/games');
      if (!response.ok) {
        if (response.status === 401) { router.push('/auth/login'); return; }
        throw new Error('Failed to fetch games');
      }
      const data = await response.json();
      setGames(data.games || []);
    } catch (err) {
      setError('Failed to load your games. Please try again.');
      console.error(err);
    } finally {
      setIsLoading(false);
    }
  }, [router]);

  useEffect(() => {
    fetchGames();
  }, [fetchGames]);

  const formatDate = (dateString: string) => {
    const date = new Date(dateString);
    return date.toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' });
  };

  // Inline skeleton for the isLoading branch (matches loading.tsx — prevents flash per Pitfall 3)
  if (isLoading) {
    return (
      <div className="min-h-screen bg-gray-50">
        <Header title="My Games" />
        <main className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
          <div className="bg-white rounded-lg shadow-sm border border-gray-200 overflow-hidden">
            <div className="h-12 bg-gray-50 border-b border-gray-200" />
            {[...Array(8)].map((_, i) => (
              <div key={i} className="flex gap-6 px-6 py-4 border-b border-gray-100">
                <div className="h-4 w-20 bg-gray-200 rounded skeleton-shimmer" />
                <div className="h-4 w-32 bg-gray-200 rounded skeleton-shimmer" />
                <div className="h-4 w-12 bg-gray-200 rounded skeleton-shimmer" />
                <div className="h-4 w-10 bg-gray-200 rounded skeleton-shimmer" />
              </div>
            ))}
          </div>
        </main>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-gray-50">
      <Header title="My Games" />
      <main className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">

        {/* Error state — inline, below Header so Logout remains accessible */}
        {error && (
          <div className="bg-white rounded-lg shadow-sm border border-red-200 p-8 text-center mb-8">
            <AlertCircle className="w-10 h-10 text-red-500 mx-auto mb-3" />
            <p className="text-red-600 font-medium mb-4">{error}</p>
            <button
              onClick={fetchGames}
              className="bg-blue-600 text-white px-6 py-2 rounded-lg hover:bg-blue-700 transition-colors"
            >
              Try Again
            </button>
          </div>
        )}

        {/* Empty state */}
        {!error && games.length === 0 && (
          <div className="bg-white rounded-lg shadow-sm border border-gray-200 p-12 text-center">
            <ClipboardList className="w-12 h-12 text-gray-400 mx-auto mb-4" />
            <h2 className="text-2xl font-bold text-gray-900 mb-2">No Games Yet</h2>
            <p className="text-gray-600 mb-6">Start tracking your performance by adding your first game!</p>
            <Link
              href="/games/add"
              className="inline-block bg-blue-600 text-white px-6 py-3 rounded-lg font-medium hover:bg-blue-700 transition-colors"
            >
              Add Your First Game
            </Link>
          </div>
        )}

        {/* Games table */}
        {!error && games.length > 0 && (
          <div className="bg-white rounded-lg shadow-sm border border-gray-200 overflow-hidden">
            <div className="flex items-center justify-between px-6 py-4 border-b border-gray-200">
              <h2 className="text-lg font-semibold text-gray-900">All Games</h2>
              <Link
                href="/games/add"
                className="bg-blue-600 text-white px-4 py-2 rounded-lg text-sm font-medium hover:bg-blue-700 transition-colors"
              >
                Add Game
              </Link>
            </div>
            <div className="overflow-x-auto">
              <table className="w-full text-sm">
                <thead className="bg-gray-50 border-b border-gray-200">
                  <tr>
                    <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Date</th>
                    <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Opponent</th>
                    <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Result</th>
                    <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">G</th>
                    <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">A</th>
                    <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">+/-</th>
                  </tr>
                </thead>
                <tbody className="divide-y divide-gray-100">
                  {games.map((game) => (
                    <tr
                      key={game.id}
                      className="hover:bg-gray-50 cursor-pointer transition-colors"
                      onClick={() => router.push(`/games/${game.id}`)}
                    >
                      <td className="px-6 py-4 text-gray-900">{formatDate(game.date)}</td>
                      <td className="px-6 py-4 text-gray-900">{game.opponent}</td>
                      <td className="px-6 py-4">
                        <span className={`font-medium capitalize ${game.result === 'win' ? 'text-green-600' : 'text-red-600'}`}>
                          {game.result === 'win' ? 'Win' : 'Loss'}
                        </span>
                      </td>
                      <td className="px-6 py-4 text-gray-900">{game.goals}</td>
                      <td className="px-6 py-4 text-gray-900">{game.assists}</td>
                      <td className="px-6 py-4 text-gray-900">
                        <span className={game.plusMinus > 0 ? 'text-green-600' : game.plusMinus < 0 ? 'text-red-600' : ''}>
                          {game.plusMinus > 0 ? '+' : ''}{game.plusMinus}
                        </span>
                      </td>
                    </tr>
                  ))}
                </tbody>
              </table>
            </div>
          </div>
        )}

      </main>
    </div>
  );
}
```

Key implementation notes:
- `isLoading` starts as `true` (same as dashboard pattern — `useState(true)`)
- `fetchGames` sets `setIsLoading(true)` and `setError('')` at the start — required for retry to show loading state
- Error state renders INSIDE the normal layout (below Header), not as a full-screen replacement
- Both error and empty states check the other condition (`!error && games.length === 0`) to avoid showing both
- The isLoading branch also renders Header first, then the skeleton — consistent layout regardless of state
- Icon choices (AlertCircle, ClipboardList) are Claude's discretion per CONTEXT.md
  </action>
  <verify>
    <automated>npm test -- --passWithNoTests</automated>
  </verify>
  <done>app/games/page.tsx exists, imports Header and shows it in all states, fetchGames uses setIsLoading(true) + setError('') at start, retry button calls fetchGames directly, empty state links to /games/add, npm test passes</done>
</task>

</tasks>

<verification>
After task completes:
1. File exists: `ls app/games/page.tsx`
2. Header import present: `grep -n "import Header" app/games/page.tsx`
3. fetchGames resets loading: `grep -n "setIsLoading(true)" app/games/page.tsx`
4. Retry button calls fetchGames: `grep -n "onClick.*fetchGames" app/games/page.tsx`
5. No TypeScript errors: `npx tsc --noEmit`
6. Regression: `npm test -- --passWithNoTests` passes
</verification>

<success_criteria>
- app/games/page.tsx is a working 'use client' page with full lifecycle
- Header renders in loading/error/empty/content states (Header always visible)
- isLoading branch shows skeleton using skeleton-shimmer class (consistent with loading.tsx)
- Error card is inline (not full-screen), has retry button calling fetchGames()
- Empty state has CTA link to /games/add
- Content table shows all games (not sliced to 5)
- npm test passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-polish/06-03-SUMMARY.md` summarizing the new page created and confirming all four lifecycle states are implemented.
</output>
