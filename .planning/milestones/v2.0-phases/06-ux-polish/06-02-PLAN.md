---
phase: 06-ux-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - app/dashboard/loading.tsx
  - app/games/loading.tsx
  - app/games/[id]/loading.tsx
autonomous: true
requirements:
  - UX-02

must_haves:
  truths:
    - "Navigating to /dashboard shows a shimmer skeleton matching the page layout (header + 4 stat cards + table rows) before data loads"
    - "Navigating to /games shows a shimmer skeleton matching the games list layout before data loads"
    - "Navigating to /games/[id] shows a shimmer skeleton matching the game detail layout before data loads"
    - "All three skeleton files use the skeleton-shimmer CSS class from app/globals.css"
    - "No loading.tsx files use 'use client' — they are Server Components"
  artifacts:
    - path: "app/dashboard/loading.tsx"
      provides: "Dashboard route-level skeleton"
      contains: "skeleton-shimmer"
    - path: "app/games/loading.tsx"
      provides: "Games list route-level skeleton"
      contains: "skeleton-shimmer"
    - path: "app/games/[id]/loading.tsx"
      provides: "Game detail route-level skeleton"
      contains: "skeleton-shimmer"
  key_links:
    - from: "app/dashboard/loading.tsx"
      to: "app/globals.css"
      via: "skeleton-shimmer CSS class"
      pattern: "skeleton-shimmer"
    - from: "app/games/[id]/loading.tsx"
      to: "app/games/[id]/page.tsx"
      via: "key={gameId} forces re-trigger on same-segment navigation"
      pattern: "key=\\{gameId\\}"
---

<objective>
Create the three route-level loading.tsx skeleton files for /dashboard, /games, and /games/[id].

Purpose: Next.js App Router automatically shows loading.tsx before the page component hydrates. This eliminates the blank white screen during initial navigation (UX-02). The shimmer animation comes from the .skeleton-shimmer class added in plan 01.

Output: Three new files — app/dashboard/loading.tsx, app/games/loading.tsx, app/games/[id]/loading.tsx
</objective>

<execution_context>
@C:/Users/jackl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jackl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-ux-polish/06-CONTEXT.md
@.planning/phases/06-ux-polish/06-RESEARCH.md
@.planning/phases/06-ux-polish/06-01-SUMMARY.md

<interfaces>
<!-- From app/globals.css (added in plan 01) -->
/* CSS class to use on all skeleton placeholder divs: */
.skeleton-shimmer {
  background: linear-gradient(90deg, #e5e7eb 25%, #f3f4f6 50%, #e5e7eb 75%);
  background-size: 200% 100%;
  animation: shimmer 1.5s infinite linear;
}

<!-- From app/dashboard/page.tsx — actual layout to match: -->
<!-- Header: flex justify-between items-center (title left, logout right) -->
<!-- Main: max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 -->
<!--   Grid: grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mb-8 (4 stat cards) -->
<!--   Table: bg-white rounded-lg shadow-sm border border-gray-200 overflow-hidden -->
<!--     Header row + 5 data rows -->

<!-- From app/games/[id]/page.tsx — actual layout to match: -->
<!-- No separate header — currently uses inline py-8 px-4 wrapper -->
<!-- After plan 04 wires Header, this page will have: Header + back link + content sections -->
<!-- Skeleton should show: header bar + back link placeholder + 2 content card blocks -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard and games list loading skeletons</name>
  <files>app/dashboard/loading.tsx, app/games/loading.tsx</files>
  <action>
Create two new Server Component files (no 'use client' directive). These are picked up automatically by Next.js App Router.

**app/dashboard/loading.tsx** — mirrors the dashboard layout (header + 4 stat cards + table):

```tsx
export default function DashboardLoading() {
  return (
    <div className="min-h-screen bg-gray-50">
      {/* Skeleton header */}
      <div className="bg-white shadow-sm border-b border-gray-200">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
          <div className="flex justify-between items-center">
            <div className="h-8 w-56 bg-gray-200 rounded skeleton-shimmer" />
            <div className="h-5 w-12 bg-gray-200 rounded skeleton-shimmer" />
          </div>
        </div>
      </div>
      {/* Skeleton main */}
      <main className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* 4 stat cards */}
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mb-8">
          {[...Array(4)].map((_, i) => (
            <div key={i} className="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
              <div className="h-4 w-24 bg-gray-200 rounded skeleton-shimmer mb-3" />
              <div className="h-10 w-16 bg-gray-200 rounded skeleton-shimmer" />
            </div>
          ))}
        </div>
        {/* Recent games table */}
        <div className="bg-white rounded-lg shadow-sm border border-gray-200 overflow-hidden">
          <div className="h-12 bg-gray-50 border-b border-gray-200" />
          {[...Array(5)].map((_, i) => (
            <div key={i} className="flex gap-6 px-6 py-4 border-b border-gray-100">
              <div className="h-4 w-20 bg-gray-200 rounded skeleton-shimmer" />
              <div className="h-4 w-28 bg-gray-200 rounded skeleton-shimmer" />
              <div className="h-4 w-12 bg-gray-200 rounded skeleton-shimmer" />
              <div className="h-4 w-10 bg-gray-200 rounded skeleton-shimmer" />
            </div>
          ))}
        </div>
      </main>
    </div>
  );
}
```

**app/games/loading.tsx** — mirrors the games list layout (header + table rows):

```tsx
export default function GamesLoading() {
  return (
    <div className="min-h-screen bg-gray-50">
      {/* Skeleton header */}
      <div className="bg-white shadow-sm border-b border-gray-200">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
          <div className="flex justify-between items-center">
            <div className="h-8 w-28 bg-gray-200 rounded skeleton-shimmer" />
            <div className="h-5 w-12 bg-gray-200 rounded skeleton-shimmer" />
          </div>
        </div>
      </div>
      <main className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <div className="bg-white rounded-lg shadow-sm border border-gray-200 overflow-hidden">
          <div className="h-12 bg-gray-50 border-b border-gray-200" />
          {[...Array(8)].map((_, i) => (
            <div key={i} className="flex gap-6 px-6 py-4 border-b border-gray-100">
              <div className="h-4 w-20 bg-gray-200 rounded skeleton-shimmer" />
              <div className="h-4 w-32 bg-gray-200 rounded skeleton-shimmer" />
              <div className="h-4 w-12 bg-gray-200 rounded skeleton-shimmer" />
              <div className="h-4 w-10 bg-gray-200 rounded skeleton-shimmer" />
            </div>
          ))}
        </div>
      </main>
    </div>
  );
}
```
  </action>
  <verify>
    <automated>npm test -- --passWithNoTests</automated>
  </verify>
  <done>Both files exist with no 'use client', both use skeleton-shimmer class, npm test passes</done>
</task>

<task type="auto">
  <name>Task 2: Create game detail loading skeleton</name>
  <files>app/games/[id]/loading.tsx</files>
  <action>
Create `app/games/[id]/loading.tsx` — a Server Component skeleton for the game detail page.

After plan 04 wires the Header into the game detail page, the page will have: Header at the top, a back link below it, then content cards. The skeleton should mirror this structure.

Note on key prop: This loading.tsx is created here as a pure addition. The `key={gameId}` that re-triggers this skeleton on same-segment navigation (/games/1 → /games/2) is applied in plan 04 when modifying app/games/[id]/page.tsx. These two changes work together — this file is the skeleton that shows, plan 04 adds the trigger mechanism.

```tsx
export default function GameDetailLoading() {
  return (
    <div className="min-h-screen bg-gray-50">
      {/* Skeleton header */}
      <div className="bg-white shadow-sm border-b border-gray-200">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
          <div className="flex justify-between items-center">
            <div className="h-8 w-48 bg-gray-200 rounded skeleton-shimmer" />
            <div className="h-5 w-12 bg-gray-200 rounded skeleton-shimmer" />
          </div>
        </div>
      </div>
      <main className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Back link placeholder */}
        <div className="h-5 w-32 bg-gray-200 rounded skeleton-shimmer mb-6" />
        {/* Title placeholder */}
        <div className="h-9 w-72 bg-gray-200 rounded skeleton-shimmer mb-2" />
        <div className="h-6 w-24 bg-gray-200 rounded skeleton-shimmer mb-8" />
        {/* Stats card */}
        <div className="bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-6">
          <div className="h-6 w-40 bg-gray-200 rounded skeleton-shimmer mb-6" />
          <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
            {[...Array(6)].map((_, i) => (
              <div key={i}>
                <div className="h-4 w-16 bg-gray-200 rounded skeleton-shimmer mb-2" />
                <div className="h-9 w-12 bg-gray-200 rounded skeleton-shimmer" />
              </div>
            ))}
          </div>
        </div>
        {/* Mental state card */}
        <div className="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
          <div className="h-6 w-32 bg-gray-200 rounded skeleton-shimmer mb-6" />
          <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
            {[...Array(4)].map((_, i) => (
              <div key={i}>
                <div className="h-4 w-20 bg-gray-200 rounded skeleton-shimmer mb-2" />
                <div className="h-9 w-10 bg-gray-200 rounded skeleton-shimmer" />
              </div>
            ))}
          </div>
        </div>
      </main>
    </div>
  );
}
```
  </action>
  <verify>
    <automated>npm test -- --passWithNoTests</automated>
  </verify>
  <done>app/games/[id]/loading.tsx exists with no 'use client', uses skeleton-shimmer class, npm test passes</done>
</task>

</tasks>

<verification>
After both tasks:
1. All three loading files exist: `ls app/dashboard/loading.tsx app/games/loading.tsx app/games/[id]/loading.tsx`
2. None have 'use client': `grep -l "use client" app/dashboard/loading.tsx app/games/loading.tsx app/games/[id]/loading.tsx` — should return nothing
3. All use skeleton-shimmer: `grep -c "skeleton-shimmer" app/dashboard/loading.tsx app/games/loading.tsx app/games/[id]/loading.tsx`
4. Regression: `npm test -- --passWithNoTests` passes
</verification>

<success_criteria>
- Three loading.tsx files created at correct paths
- Each is a Server Component (no 'use client')
- Each uses skeleton-shimmer class (defined in plan 01's globals.css change)
- Layout fidelity: dashboard has 4 cards + table; games has table rows; [id] has header + 2 cards
- npm test passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-polish/06-02-SUMMARY.md` summarizing the three skeleton files created and their layouts.
</output>
