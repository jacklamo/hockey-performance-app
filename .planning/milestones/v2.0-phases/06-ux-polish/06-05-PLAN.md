---
phase: 06-ux-polish
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - app/games/add/page.tsx
  - app/games/[id]/edit/page.tsx
  - app/games/[id]/mental/page.tsx
autonomous: true
requirements:
  - UX-03

must_haves:
  truths:
    - "Add game submit button shows Loader2 + 'Adding...' while isLoading=true and is disabled"
    - "Edit game submit button shows Loader2 + 'Saving...' while isSaving=true and is disabled"
    - "Mental state submit button shows Loader2 + 'Saving...' while isLoading=true and is disabled"
    - "PDF import label shows Loader2 + 'Importing...' while isImporting=true"
    - "No double-submit is possible — disabled={isLoading|isSaving} prevents re-clicks"
  artifacts:
    - path: "app/games/add/page.tsx"
      provides: "Add game form with Loader2 submit feedback"
      contains: "Loader2"
    - path: "app/games/[id]/edit/page.tsx"
      provides: "Edit game form with Loader2 submit feedback"
      contains: "Loader2"
    - path: "app/games/[id]/mental/page.tsx"
      provides: "Mental state form with Loader2 submit feedback"
      contains: "Loader2"
  key_links:
    - from: "app/games/add/page.tsx submit button"
      to: "isLoading state"
      via: "disabled={isLoading} + Loader2 conditional"
      pattern: "isLoading.*Loader2|Loader2.*isLoading"
    - from: "app/games/[id]/edit/page.tsx submit button"
      to: "isSaving state"
      via: "disabled={isSaving} + Loader2 conditional"
      pattern: "isSaving.*Loader2|Loader2.*isSaving"
    - from: "app/games/[id]/mental/page.tsx submit button"
      to: "isLoading state"
      via: "disabled={isLoading} + Loader2 conditional"
      pattern: "isLoading.*Loader2|Loader2.*isLoading"
---

<objective>
Add Loader2 spinner feedback to all three form submit buttons: add game, edit game, and mental state check-in.

Purpose: These form pages already have the correct loading state variables (isLoading, isSaving) and disabled attributes. This plan is purely mechanical wiring — import Loader2 and update the button content to show the icon + text change while submitting. This closes UX-03 for all form pages (delete button is handled in plan 04).

Output: Three pages modified in-place — only the submit button content and Loader2 import change.
</objective>

<execution_context>
@C:/Users/jackl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jackl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-ux-polish/06-CONTEXT.md
@.planning/phases/06-ux-polish/06-RESEARCH.md
@app/games/add/page.tsx
@app/games/[id]/edit/page.tsx
@app/games/[id]/mental/page.tsx

<interfaces>
<!-- Loader2 from lucide-react (already installed) -->
import { Loader2 } from 'lucide-react';
// Usage:
<Loader2 className="w-4 h-4 animate-spin" />
// animate-spin is a Tailwind CSS 4 utility — already installed

<!-- Add page current imports (line 6): -->
import { ArrowLeft, FileText } from 'lucide-react';
// → change to: import { ArrowLeft, FileText, Loader2 } from 'lucide-react';

<!-- Edit page current imports: no lucide-react import yet -->
// → add: import { Loader2 } from 'lucide-react';

<!-- Mental page current imports: no lucide-react import yet -->
// → add: import { Loader2 } from 'lucide-react';

<!-- Locked text mapping (from CONTEXT.md): -->
// Add game submit: idle="Save Game" submitting=Loader2+"Adding..."
// Edit game submit: idle="Save Changes" submitting=Loader2+"Saving..."
// Mental state submit: idle="Save Check-In" submitting=Loader2+"Saving..."
// PDF import label: idle="Import from Instat PDF" importing=Loader2+"Importing..."

<!-- Current add page submit button (lines ~507-513): -->
<button type="submit" disabled={isLoading}>
  {isLoading ? 'Saving...' : 'Save Game'}
</button>
// Change text to 'Adding...' + add Loader2

<!-- Current add page PDF label (line ~199): -->
<label className={`cursor-pointer inline-flex items-center gap-2 ... ${isImporting || isLoading ? 'opacity-50 cursor-not-allowed' : ''}`}>
  {isImporting ? 'Parsing...' : 'Import from Instat PDF'}
// Change 'Parsing...' to Loader2 + 'Importing...'

<!-- Current edit page submit button (lines ~483-487): -->
<button type="submit" disabled={isSaving}>
  {/* currently shows text only — need to verify exact current text */}
</button>

<!-- Current mental page submit button (lines ~295-300): -->
<button type="submit" disabled={isLoading || showSuccess}>
  {isLoading ? 'Saving...' : 'Save Check-In'}
</button>
// → add Loader2 when isLoading
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Loader2 to add game form (add/page.tsx)</name>
  <files>app/games/add/page.tsx</files>
  <action>
Read app/games/add/page.tsx first, then make these targeted changes only — do not change anything else.

**1. Update the lucide-react import (line ~6):**
Current: `import { ArrowLeft, FileText } from 'lucide-react';`
Change to: `import { ArrowLeft, FileText, Loader2 } from 'lucide-react';`

**2. Update the submit button (near the end of the file, around the `type="submit"` button):**
Current text: `{isLoading ? 'Saving...' : 'Save Game'}`

Note: Per locked decisions, the add game button's submitting text should be "Adding..." (not "Saving..."). The idle text stays "Save Game".

Replace button content with:
```tsx
{isLoading ? (
  <>
    <Loader2 className="w-4 h-4 animate-spin inline mr-2" />
    Adding...
  </>
) : (
  'Save Game'
)}
```

Keep `type="submit"`, `disabled={isLoading}`, and all existing className attributes unchanged.

**3. Update the PDF import label (around line 199):**
Current: `{isImporting ? 'Parsing...' : 'Import from Instat PDF'}`
Replace with:
```tsx
{isImporting ? (
  <>
    <Loader2 className="w-4 h-4 animate-spin" />
    Importing...
  </>
) : (
  <>
    <FileText className="w-4 h-4" />
    Import from Instat PDF
  </>
)}
```
Note: The label already contains a `<FileText>` icon via `gap-2` — check the actual current markup. If FileText is already inline before the text, preserve that structure and only add the Loader2 conditional. The label has `inline-flex items-center gap-2` so the flex layout handles spacing.

Do not touch any other parts of the file.
  </action>
  <verify>
    <automated>npm test -- --passWithNoTests</automated>
  </verify>
  <done>add/page.tsx imports Loader2, submit button shows Loader2 + 'Adding...' when isLoading, PDF label shows Loader2 + 'Importing...' when isImporting, npm test passes</done>
</task>

<task type="auto">
  <name>Task 2: Add Loader2 to edit and mental state forms</name>
  <files>app/games/[id]/edit/page.tsx, app/games/[id]/mental/page.tsx</files>
  <action>
Read both files first, then make targeted changes to each.

**app/games/[id]/edit/page.tsx:**

Read the file to find the current lucide-react import (if any) and the submit button. Based on code audit, this file has no lucide-react import yet.

1. Add import at the top (after existing imports):
   ```tsx
   import { Loader2 } from 'lucide-react';
   ```

2. Find the submit button with `type="submit"` and `disabled={isSaving}`. Read the current button text content (the code audit shows it's around line 483-487 but verify).

3. Replace the button's text/children with:
   ```tsx
   {isSaving ? (
     <>
       <Loader2 className="w-4 h-4 animate-spin inline mr-2" />
       Saving...
     </>
   ) : (
     'Save Changes'
   )}
   ```

4. Ensure the button has `className` that includes `disabled:opacity-50 disabled:cursor-not-allowed`. If the current className is missing these, add them. Keep all other className values unchanged.

---

**app/games/[id]/mental/page.tsx:**

Read the file to find the current lucide-react import (if any) and the submit button. Based on code audit, this file has no lucide-react import yet.

1. Add import at the top:
   ```tsx
   import { Loader2 } from 'lucide-react';
   ```

2. Find the submit button with `type="submit"` and `disabled={isLoading || showSuccess}`. Current text: `{isLoading ? 'Saving...' : 'Save Check-In'}`.

3. Replace with:
   ```tsx
   {isLoading ? (
     <>
       <Loader2 className="w-4 h-4 animate-spin inline mr-2" />
       Saving...
     </>
   ) : (
     'Save Check-In'
   )}
   ```

4. Keep `disabled={isLoading || showSuccess}` and all existing className attributes unchanged.

Do not touch sliders, form inputs, validation, or any other part of either file.
  </action>
  <verify>
    <automated>npm test -- --passWithNoTests</automated>
  </verify>
  <done>edit/page.tsx imports Loader2 and shows Loader2 + 'Saving...' when isSaving; mental/page.tsx imports Loader2 and shows Loader2 + 'Saving...' when isLoading; npm test passes</done>
</task>

</tasks>

<verification>
After both tasks:
1. All three pages import Loader2: `grep -n "Loader2" app/games/add/page.tsx app/games/[id]/edit/page.tsx app/games/[id]/mental/page.tsx`
2. Add page uses 'Adding...': `grep -n "Adding" app/games/add/page.tsx`
3. PDF label uses 'Importing...': `grep -n "Importing" app/games/add/page.tsx`
4. Edit page uses 'Saving...': `grep -n "Saving" app/games/[id]/edit/page.tsx`
5. Mental page uses 'Saving...': `grep -n "Saving" app/games/[id]/mental/page.tsx`
6. TypeScript: `npx tsc --noEmit`
7. Regression: `npm test -- --passWithNoTests` passes
</verification>

<success_criteria>
- All three form pages import Loader2 from lucide-react
- Add game: submit shows Loader2 + "Adding..." while isLoading, PDF label shows Loader2 + "Importing..." while isImporting
- Edit game: submit shows Loader2 + "Saving..." while isSaving
- Mental state: submit shows Loader2 + "Saving..." while isLoading
- All submit buttons remain disabled during submission (existing disabled= attributes preserved)
- Zero changes to form logic, validation, or state management
- npm test passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-polish/06-05-SUMMARY.md` summarizing the three files modified and confirming Loader2 is wired to each form's existing loading state variable.
</output>
