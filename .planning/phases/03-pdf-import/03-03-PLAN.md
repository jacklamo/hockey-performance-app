---
phase: 03-pdf-import
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/lib/__tests__/parse-instat-pdf.test.ts
autonomous: true
gap_closure: true
requirements:
  - PDF-02

must_haves:
  truths:
    - "All 8 unit tests in parse-instat-pdf.test.ts pass green"
    - "npx jest src/lib/__tests__/parse-instat-pdf.test.ts exits 0"
    - "homeAway field extraction is covered by a dedicated test case"
  artifacts:
    - path: "src/lib/__tests__/parse-instat-pdf.test.ts"
      provides: "Unit test contract aligned with real Instat PDF regex patterns"
      contains: "Goals 2"
      exports: ["describe('parseInstatPdf')"]
  key_links:
    - from: "src/lib/__tests__/parse-instat-pdf.test.ts"
      to: "src/lib/parse-instat-pdf.ts"
      via: "mockPdfParse fixture strings matching parser regexes"
      pattern: "Goals\\s+\\d+"
---

<objective>
Realign the parser unit tests with the updated real-Instat-format regex patterns so the test suite is green.

Purpose: The parser was correctly updated to match actual Instat PDF output (no colons, European date format, shots-on-goal format, two-team header for opponent/homeAway detection). The test fixtures were not updated in sync, causing 6 of 7 tests to fail. This plan fixes that drift so the automated test contract verifies the parser's real behavior.
Output: Updated `src/lib/__tests__/parse-instat-pdf.test.ts` with all 8 tests passing.
</objective>

<execution_context>
@C:/Users/jackl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jackl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pdf-import/03-VERIFICATION.md

@src/lib/parse-instat-pdf.ts
@src/lib/__tests__/parse-instat-pdf.test.ts

<interfaces>
<!-- Current parser signatures and regex patterns. Executor must align test fixtures to these. -->

From src/lib/parse-instat-pdf.ts:

```typescript
export interface InstatFields {
  goals?: string;
  assists?: string;
  shots?: string;
  opponent?: string;
  homeAway?: 'home' | 'away';
  date?: string;
  plusMinus?: string;
  iceTime?: string;
}

export async function parseInstatPdf(buffer: Buffer): Promise<InstatFields>
```

Active regex patterns (executor must match fixture strings to these):
- Goals:    /^Goals\s+(\d+)/im        — "Goals 2" (space, no colon, line-start anchored)
- Assists:  /^Assists\s+(\d+)/im      — "Assists 1" (space, no colon, line-start anchored)
- Shots:    /Shots\s*\/\s*on\s+goal\s+(\d+)\s*\/\s*\d+/i  — "Shots / on goal 5 / 3"
- Date:     /(\d{2})\.(\d{2})\.(\d{4})/  — "15.03.2024" → output "2024-03-15"
- Opponent/homeAway: two-step —
    Step 1: /^(.+?)\s+(\d+):(\d+)\s+(.+)$/m  matches "TEAM_A 2:1 TEAM_B"
    Step 2: checks whether homeTeam or awayTeam appears alone on its own line
    - If awayTeam appears standalone: opponent=homeTeam, homeAway='away'
    - If homeTeam appears standalone: opponent=awayTeam, homeAway='home'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update test fixtures to match real Instat PDF patterns and add homeAway test</name>
  <files>src/lib/__tests__/parse-instat-pdf.test.ts</files>
  <action>
    Rewrite the 7 existing test fixture strings and add 1 new test so all 8 pass against the current parser.

    Keep the mock setup unchanged (jest.mock at top, mockPdfParse.mockReset() in beforeEach, makePdfData helper). Only change fixture strings and expected values.

    **Test 1 — goals:** Change fixture from `'Goals: 2\n'` to `'Goals 2\n'`. Expected: `{ goals: '2' }`.

    **Test 2 — assists:** Change fixture from `'Assists: 1\n'` to `'Assists 1\n'`. Expected: `{ assists: '1' }`.

    **Test 3 — shots:** Change fixture from `'Shots: 5\n'` to `'Shots / on goal 5 / 3 50%\n'`. Expected: `{ shots: '5' }`. (The regex captures the first number — total shots.)

    **Test 4 — opponent (home game):** The old `'vs. Team Name\n'` fixture no longer matches. Replace with a two-line fixture that exercises the home team being the player's team:
    ```
    'TEAM_A 2:1 TEAM_B\nTEAM_A\n'
    ```
    The header is `TEAM_A 2:1 TEAM_B`. `TEAM_A` appears standalone on its own line (the player's team). So `homeTeam=TEAM_A` is standalone → `opponent='TEAM_B'`, `homeAway='home'`.
    Expected: `expect.objectContaining({ opponent: 'TEAM_B', homeAway: 'home' })`.
    Update the test name to `'extracts opponent and homeAway=home from header when home team appears standalone'`.

    **Test 5 — date:** Change fixture from `'2024-03-15\n'` to `'15.03.2024\n'`. Expected: `{ date: '2024-03-15' }`. (Parser reformats DD.MM.YYYY → YYYY-MM-DD.)

    **Test 6 — empty/no fields:** Keep `'No stats here.\n'` fixture unchanged. Expected: `{}`. This test already passes.

    **Test 7 — partial extraction:** Change fixtures from `'Goals: 1\nAssists: 2\n'` to `'Goals 1\nAssists 2\n'`. Expected: `objectContaining({ goals: '1', assists: '2' })` and `not.toHaveProperty('shots')`.

    **Test 8 — homeAway away (new test):** Add a new test for the away case:
    ```typescript
    test('extracts opponent and homeAway=away from header when away team appears standalone', async () => {
      mockPdfParse.mockResolvedValue(makePdfData('TEAM_A 2:1 TEAM_B\nTEAM_B\n'));
      const result = await parseInstatPdf(Buffer.from(''));
      expect(result).toEqual(expect.objectContaining({ opponent: 'TEAM_A', homeAway: 'away' }));
    });
    ```
    Here `TEAM_B` (awayTeam) appears standalone → `opponent='TEAM_A'`, `homeAway='away'`.

    Do NOT change the mock setup, import paths, `makePdfData` helper, or `describe`/`beforeEach` structure.
  </action>
  <verify>
    <automated>npx jest src/lib/__tests__/parse-instat-pdf.test.ts --no-coverage</automated>
  </verify>
  <done>Test suite reports 8 passed, 0 failed. `npx jest src/lib/__tests__/parse-instat-pdf.test.ts` exits 0.</done>
</task>

</tasks>

<verification>
- `src/lib/__tests__/parse-instat-pdf.test.ts` has 8 test cases (7 updated + 1 new homeAway away test)
- All fixture strings match the active parser regex patterns in `src/lib/parse-instat-pdf.ts`
- `npx jest src/lib/__tests__/parse-instat-pdf.test.ts` reports 8 passed, 0 failed, exits 0
- `npx jest` (full suite) exits 0 (route tests continue to pass)
</verification>

<success_criteria>
The parser unit test contract is aligned with the real Instat PDF format. All 8 tests pass. The automated verification for PDF-02 (app parses PDF and pre-fills form fields) is fully green. The regression introduced by the parser update without a corresponding test update is closed.
</success_criteria>

<output>
After completion, create `.planning/phases/03-pdf-import/03-03-SUMMARY.md`
</output>
